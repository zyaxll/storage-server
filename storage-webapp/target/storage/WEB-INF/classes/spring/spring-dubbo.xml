<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
		http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd">

    <context:property-placeholder location="classpath:config.properties" />

    <dubbo:application name="goods-server"/>
    <dubbo:registry protocol="zookeeper" address="${dubbo.zookeeper.address}"/>
    <dubbo:monitor protocol="registry"/>

    <dubbo:protocol port="${dubbo.server.port}" accepts="1000" threads="400" threadpool="fixed"/>

    <!--<dubbo:reference id="dubboEmailService" interface="com.b5m.sms.service.EmailService" check="false" />-->

    <!--
    <dubbo:service ref="categoryService" interface="com.b5m.goods.service.core.ICategoryService" version="${dubbo.server.version}">
        <dubbo:method name="save" retries="0"></dubbo:method>
        <dubbo:method name="saveOrUpdate" retries="0"></dubbo:method>
        <dubbo:method name="update" retries="0"></dubbo:method>
        <dubbo:method name="updateAttribute" retries="0"></dubbo:method>
        <dubbo:method name="delete" retries="0"></dubbo:method>
        <dubbo:method name="deleteAll" retries="0"></dubbo:method>
        <dubbo:method name="deleteByPropertyAndValue" retries="0"></dubbo:method>
        <dubbo:method name="deleteByPropertyValueMap" retries="0"></dubbo:method>
    </dubbo:service>
    -->

</beans>
